# The Move is aarch64 glibc. Link with the same cross-gcc the C build uses, so
# the two artifacts resolve against one sysroot and cannot disagree about libc.
[target.aarch64-unknown-linux-gnu]
linker = "aarch64-linux-gnu-gcc"
